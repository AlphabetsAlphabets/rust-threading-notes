use std::cell::RefCell;

fn main() {
    // RefCell lets you borrow its contents and keeps a counter of borrows.
    // It will let you make as many shared references as you'd like but
    // you can only have one exclusive reference.
}
